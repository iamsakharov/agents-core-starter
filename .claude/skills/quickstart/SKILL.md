---
name: quickstart
description: Use when the user describes a new task, goal, or project for the first time and wants to start building. Immediately activates plan-orchestrator to analyze, decompose and plan the work.
---

# quickstart

Единый источник истины этого skill — `.cursor/skills/quickstart/SKILL.md`.

Прочитай его (Read `.cursor/skills/quickstart/SKILL.md`) и работай строго по нему. Он синхронизирован с правилами Cursor — здесь намеренно нет дубля, чтобы избежать расхождений.

Точка входа для новой задачи: активирует `plan-orchestrator` (в Claude — субагент `plan-orchestrator`) для анализа и декомпозиции. Соблюдай общие правила проекта из `CLAUDE.md`.
